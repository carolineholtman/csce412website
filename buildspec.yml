version: 0.2

phases:
  build:
    commands:
      - echo "Deploying static site to S3..."
      - aws s3 sync . s3://csce412carolineholtman.me \
        --delete \
        --exclude ".git/*" \
        --exclude "buildspec.yml"
artifacts:
  files:
    - '**/*'
